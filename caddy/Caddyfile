https://localhost {
  
  # Goサーバーへのリバースプロキシ
  reverse_proxy /api/* go-server:8000

  # Next.js フロントエンドへのリバースプロキシ
  reverse_proxy / frontend:3000

  # ログ出力（任意）
  log {
    output file /var/log/caddy/access.log
    format single_field common_log
  }
}